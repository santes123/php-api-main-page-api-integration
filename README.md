# php-api-main-page-api-integration
Base completa para una API REST en PHP sin frameworks, con una arquitectura modular y escalable que incluye enrutamiento, controladores, autenticación JWT, migraciones y gestión del flujo HTTP mediante middlewares. Utiliza Composer y tiene interfaz web con el flujo completo de autenticación, CRUD de tareas e integración para la API.
